/**
 * Dual-texture hero — product read vs packaging sand.
 */
export const DUAL_TEXTURE_PALETTE = {
  /** Setting powder — deep toasted wheat, tinted matte */
  powder: "#C9A780",
  /** Cream bronzer — golden bronze, high-gloss / specular */
  creamBronzer: "#A67344",
  /** Outer packaging / field (matches site sand) */
  packagingSand: "#DCCFBF",
} as const;
