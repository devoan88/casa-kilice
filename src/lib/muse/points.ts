/** Granted once on successful Muse registration. */
export const MUSE_REGISTRATION_BONUS = 10;

/** Added to `User.points` only when an admin sets submission to Approved. */
export const MUSE_POINTS_PHOTO_APPROVED = 5;

/** Added to `User.points` only when an admin sets submission to Approved. */
export const MUSE_POINTS_VIDEO_APPROVED = 15;
