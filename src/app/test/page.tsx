export default function TestPage() {
  return (
    <div className="p-8 font-sans text-lg">
      Hello World — if you see this, routing works; remove this route when done debugging.
    </div>
  );
}
